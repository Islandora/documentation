<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.0.0"
       xsi:schemaLocation="
       http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

  <!-- Load Configuration File -->
  <cm:property-placeholder persistent-id="islandora"/>

  <!-- Islandora Component Configuration -->
  <bean id="islandora" class="ca.islandora.camel.component.IslandoraComponent">
    <argument value="${islandora.php.workingDir}"/>
  </bean>

  <camelContext id="basicImageService" xmlns="http://camel.apache.org/schema/blueprint">

<!--
    <route>
      <from uri="jetty:http://0.0.0.0:8888/islandora/rest/basic-image?httpMethodRestrict=POST"/>
        <setProperty propertyName="uuid"><simple>${headers.uuid}</simple></setProperty>
        <setProperty propertyName="node"><simple>${headers.node}</simple></setProperty>
        <setProperty propertyName="attachments"><javaScript>request.getAttachments()</javaScript></setProperty>
        <log message="POST ATTACHMENTS: ${property.attachments}"/>
    </route>
-->
    <routeContextRef ref="get"/> 

    <route>
      <from uri="jetty:http://0.0.0.0:8888/islandora/rest/basic-image/"/>
        <choice>
          <when><simple>${headers.CamelHttpMethod} == 'GET'</simple>
            <log message="GOT A GET"/>  
            <setProperty propertyName="uuid"><simple>${headers.uuid}</simple></setProperty>
          </when>
          <when><simple>${headers.CamelHttpMethod} == 'POST'</simple>
            <log message="GOT A POST"/>  
            <setProperty propertyName="uuid"><simple>${headers.uuid}</simple></setProperty>
            <setProperty propertyName="node"><simple>${headers.node}</simple></setProperty>
            <setProperty propertyName="attachments"><javaScript>request.getAttachments()</javaScript></setProperty>
            <to uri="vm:getCollectionUri"/>
            <to uri="direct:createBasicImage"/>
          </when>
          <when><simple>${headers.CamelHttpMethod} == 'PUT'</simple>
            <log message="GOT A PUT"/>  
            <setProperty propertyName="uuid"><simple>${headers.uuid}</simple></setProperty>
            <setProperty propertyName="node"><simple>${headers.node}</simple></setProperty>
            <setProperty propertyName="attachments"><javaScript>request.getAttachments()</javaScript></setProperty>
          </when>
          <when><simple>${headers.CamelHttpMethod} == 'DELETE'</simple>
            <log message="GOT A DELETE"/>  
            <setProperty propertyName="uuid"><simple>${headers.uuid}</simple></setProperty>
          </when>
        </choice>
<!--
          <transform>
            <javaScript>
              request.getAttachments().values().toArray()[0].getContent(); 
            </javaScript>
          </transform>
-->
    </route>
<!--
    <restConfiguration component="jetty"
                       port="8181">
      <endpointProperty key="enableMultipartFilter" value="true"/>
    </restConfiguration>

    <rest path="/islandora/rest/basic-image">
      <get>
        <description>Creates a basic image in Fedora using Drupal data</description>
        <to uri="direct:getBasicImage"/>
      </get>
      <post uri="/{uuid}" consumes="multipart/form-encoded">
        <description>Creates a basic image in Fedora using Drupal data</description>
        <to uri="direct:createBasicImage"/>
      </post>
      <put uri="/{uuid}" consumes="multipart/form-encoded">
        <description>Updates a basic image in Fedora using Drupal data</description>
        <to uri="direct:updateBasicImage"/>
      </put>
    </rest>
-->

  </camelContext>

</blueprint>
