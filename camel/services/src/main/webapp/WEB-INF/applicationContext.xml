<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

  <!-- Spring - Camel Bridged Properties -->
  <bean id="bridgePropertyPlaceholder" class="org.apache.camel.spring.spi.BridgePropertyPlaceholderConfigurer">
    <property name="location" value="classpath:services.properties"/>
  </bean>
  
  <!-- JDBC data source -->
  <bean id="dataSource"
        class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}"/>
    <property name="url" value="${jdbc.url}"/>
    <property name="username" value="${jdbc.username}"/>
    <property name="password" value="${jdbc.password}"/>
  </bean>

  <!-- Hibernate config -->
  <bean id="hibernate" class="org.apacheextras.camel.component.hibernate.HibernateComponent">
    <property name="sessionFactory" ref="sessionFactory"/>
    <property name="transactionStrategy" ref="springTransactionStrategy"/>
  </bean>

  <bean id="springTransactionStrategy" class="org.apacheextras.camel.component.hibernate.SpringTransactionStrategy">
    <constructor-arg ref="sessionFactory"/>
    <constructor-arg ref="transactionTemplate"/>
  </bean>

  <bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
    <property name="sessionFactory" ref="sessionFactory"/>
  </bean>

  <bean id="transactionTemplate" class="org.springframework.transaction.support.TransactionTemplate">
    <property name="transactionManager" ref="transactionManager"/>
  </bean>

  <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    <property name="mappingResources">
      <list>
        <value>UUIDMap.hbm.xml</value>
      </list>
    </property>
    <property name="hibernateProperties">
      <value>
        hibernate.dialect=org.hibernate.dialect.MySQLDialect
        hibernate.hbm2ddl.auto=update
      </value>
    </property>
  </bean>
  <!-- End Hibernate Config -->
  
  <!-- Fedora Component Config -->
  <!-- <bean id="fcrepoTxManager" class="org.fcrepo.camel.FcrepoTransactionManager">
    <property name="baseUrl" value="http://${fcrepo.baseurl}"/>
  </bean>

  <bean id="fcrepo" class="org.fcrepo.camel.FcrepoComponent">
    <property name="transactionManager" ref="fcrepoTxManager"/>
  </bean> -->
  <!-- End Fedora Component Config -->
  
  <bean id="uuidService" class="ca.islandora.services.uuid.UUIDService">
    <constructor-arg ref="sessionFactory"/>
  </bean>

  <bean id="collectionServiceProcessor" class="ca.islandora.services.collection.CollectionServiceProcessorBean">
    <constructor-arg ref="uuidService"/>
  </bean>
  
  <camelContext xmlns="http://camel.apache.org/schema/spring">
    <package>ca.islandora.services.routes</package>
  </camelContext>

</beans>
