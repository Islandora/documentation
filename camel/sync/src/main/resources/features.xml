<?xml version='1.0' encoding='UTF-8'?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.3.0" name="${project.artifactId}-${project.version}">
  <feature name="${project.artifactId}" version="${project.version}" start-level="50">
    <details>Installs ${project.name}</details>
    <bundle start-level="80">mvn:${project.groupId}/${project.artifactId}/${project.version}</bundle>
    <feature prerequisite="true" version="${camel.version.range}">camel</feature>
    <feature prerequisite="true" version="${activemq.version}">activemq-camel</feature>
    <feature prerequisite="true" version="${fcrepo.camel.version}">fcrepo-camel</feature>
    <feature prerequisite="true" version="${project.version}">islandora-camel-component</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-http</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-cache</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-jsonpath</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-script</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-script-javascript</feature>
    <feature prerequisite="true" version="${camel.version.range}">camel-base64</feature>
  </feature>
</features>
