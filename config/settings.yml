# Rename this file to settings.yml for production use or
# rename it to settings.dev.yml to be used with $app['debug'] == TRUE
claw:
  protocol: http
  host: localhost
  port: 8282
fedora:
  protocol: http
  host: localhost
  port: 8080
  path: /fcrepo/rest/
